name: CI

# Triggers that start the workflow
on:
  push:

  pull_request:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: check-artifact
        run: scripts/check_artifact.py
  

  #docker build -t shipit3 .
  #docker run --rm shipit3  